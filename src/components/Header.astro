---
import styles from './Header.module.scss'

interface Props {
  showFontSize?: boolean
  hideSidebar?: boolean
}

const { showFontSize = false, hideSidebar = false } = Astro.props
---

<header class={styles.topbar}>
  <div class={styles['topbar-start']}>
    {!hideSidebar && (
      <button id="sidebar-toggle" class={styles['topbar-sidebar-toggle']} aria-label="Open blog list" aria-expanded="false" aria-controls="sidebar">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <line x1="4" y1="7" x2="20" y2="7"></line>
          <line x1="4" y1="12" x2="20" y2="12"></line>
          <line x1="4" y1="17" x2="20" y2="17"></line>
        </svg>
      </button>
    )}

    <a href="/" class={styles.brand}>
      <span class={styles['brand-text']}>
        <span class={styles['brand-name']}>MasterZack&apos;s</span>
        <span class={styles['brand-sub']}>JeevanKatha</span>
      </span>
    </a>
  </div>

  <div class={styles['topbar-controls']}>
    {
      showFontSize && (
        <div class={styles['fontsize-group']} role="group" aria-label="Text size">
          <button id="size-down" class={styles['size-btn']} aria-label="Decrease text size" title="Smaller text">
            A<span class={styles['size-sign']}>-</span>
          </button>
          <button id="size-reset" class={styles['size-btn']} aria-label="Reset text size" title="Default text">
            A
          </button>
          <button id="size-up" class={styles['size-btn']} aria-label="Increase text size" title="Larger text">
            A<span class={styles['size-sign']}>+</span>
          </button>
        </div>
      )
    }

    <button id="theme-toggle" class={styles['theme-btn']} aria-label="Toggle light and dark theme" title="Toggle theme">
      <svg class={styles['icon-sun']} width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
        <circle cx="12" cy="12" r="4.5"></circle>
        <line x1="12" y1="2" x2="12" y2="4.5"></line>
        <line x1="12" y1="19.5" x2="12" y2="22"></line>
        <line x1="2" y1="12" x2="4.5" y2="12"></line>
        <line x1="19.5" y1="12" x2="22" y2="12"></line>
        <line x1="4.9" y1="4.9" x2="6.7" y2="6.7"></line>
        <line x1="17.3" y1="17.3" x2="19.1" y2="19.1"></line>
        <line x1="4.9" y1="19.1" x2="6.7" y2="17.3"></line>
        <line x1="17.3" y1="6.7" x2="19.1" y2="4.9"></line>
      </svg>
      <svg class={styles['icon-moon']} width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z"></path>
      </svg>
    </button>
  </div>
</header>

<script define:vars={{ styles }}>
  const root = document.documentElement

  // --- Progressive font size controls ---
  const downBtn = document.getElementById('size-down') as HTMLButtonElement | null
  if (downBtn) {
    const MIN = 0
    const MAX = 6
    const DEFAULT = 3
    const resetBtn = document.getElementById('size-reset') as HTMLButtonElement | null
    const upBtn = document.getElementById('size-up') as HTMLButtonElement | null

    function currentLevel(): number {
      const raw = Number(root.dataset.fontsize)
      return Number.isInteger(raw) && raw >= MIN && raw <= MAX ? raw : DEFAULT
    }

    function applyLevel(level: number) {
      const clamped = Math.min(MAX, Math.max(MIN, level))
      if (clamped === DEFAULT) {
        delete root.dataset.fontsize
      } else {
        root.dataset.fontsize = String(clamped)
      }
      try {
        localStorage.setItem('jk-fontsize', String(clamped))
      } catch {}
      if (downBtn) downBtn.disabled = clamped === MIN
      if (upBtn) upBtn.disabled = clamped === MAX
      resetBtn?.classList.toggle(styles.active, clamped === DEFAULT)
    }

    downBtn.addEventListener('click', () => applyLevel(currentLevel() - 1))
    upBtn?.addEventListener('click', () => applyLevel(currentLevel() + 1))
    resetBtn?.addEventListener('click', () => applyLevel(DEFAULT))

    applyLevel(currentLevel())
  }

  // --- Theme toggle ---
  const themeBtn = document.getElementById('theme-toggle')
  themeBtn?.addEventListener('click', () => {
    const next = root.dataset.theme === 'dark' ? 'light' : 'dark'
    root.dataset.theme = next
    try {
      localStorage.setItem('jk-theme', next)
    } catch {}
  })

  // --- Sidebar toggle ---
  document.getElementById('sidebar-toggle')?.addEventListener('click', () => {
    document.dispatchEvent(new CustomEvent('sidebar:toggle'))
  })
</script>
